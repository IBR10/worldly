# dependencies (dev-only: wrangler + linters; the site itself ships no runtime deps)
node_modules/

# OS cruft
.DS_Store
Thumbs.db

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# superpowers scratch (task briefs, review packages, progress ledger)
.superpowers/
